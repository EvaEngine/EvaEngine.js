import { utils } from 'evaengine';
import schema from './schemas/<%=table%>';

module.exports = function define(sequelize, DataTypes) {
  const { columns, table } = schema(DataTypes);
  const entity = sequelize.define('<%=table%>', utils.merge(columns, {}), utils.merge(table, {}));
  return entity;
};
